name: Build and Push Development Version for TEEHR Hub

on:
  push:
    branches: [main]  # 475-update-python-dependencies

jobs:
  call-docker-build:
    uses: RTIInternational/teehr-hub/.github/workflows/build-and-push-image.yaml@update-deployment
    secrets: inherit
    with:
      image-tag: "dev"
      previous-image-tag: "dev"  # does nothing in this case