name: Build and Push Development Version for TEEHR Hub

on:
  pull_request:
    types: [closed]
# on:
#   push:
#     branches: [475-update-python-dependencies]
  # workflow_run:
  #   workflows: ["run-tests"]     # Triggered by "run-tests" workflow
  #   types: [completed]           # Trigger when "run-tests" completes

jobs:
  call-docker-build:
    uses: RTIInternational/teehr-hub/.github/workflows/build-and-push-image.yaml@update-deployment
    secrets: inherit
    with:
      image-tag: "dev"