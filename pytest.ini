[pytest]
log_cli = false
log_cli_level = INFO

# Test markers for selective test execution
markers =
    evaluation_template: Tests that require an evaluation template to be cloned.
    read_only_warehouse: Tests that only read from the data warehouse.