[pytest]
log_cli = false
log_cli_level = INFO

# Test markers for selective test execution
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    fast: marks tests as fast unit tests
    spark: marks tests that require Spark
    iceberg: marks tests that require Iceberg tables